<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="match_parent"
  android:layout_height="wrap_content"
  android:paddingBottom="30dp"
  android:background="@color/_2a2c34"
  xmlns:app="http://schemas.android.com/apk/res-auto">

  <ImageView
    android:id="@+id/oval"
    android:layout_width="6dp"
    android:layout_height="6dp"
    android:src="@drawable/oval"
    android:layout_marginTop="8dp"
    android:layout_marginLeft="26dp"
    app:layout_constraintLeft_toLeftOf="parent"
    app:layout_constraintTop_toTopOf="parent"/>

  <TextView
    android:id="@+id/date"
    android:text="11.03 2018"
    android:textSize="18sp"
    android:textColor="@color/_ffffff"
    android:layout_width="47dp"
    android:layout_height="wrap_content"
    android:maxLines="2"
    android:gravity="right"
    android:layout_marginLeft="14dp"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintLeft_toRightOf="@+id/oval"/>

  <ImageView
    android:id="@+id/comma"
    android:src="@drawable/ic_comma1"
    android:layout_width="24dp"
    android:layout_height="24dp"
    android:layout_marginTop="31dp"
    app:layout_constraintTop_toBottomOf="@+id/date"
    app:layout_constraintLeft_toLeftOf="@+id/date"/>

  <ImageView
    android:id="@+id/my_log_image"
    android:layout_width="234dp"
    android:layout_height="58dp"
    android:scaleType="fitXY"
    app:layout_constraintRight_toRightOf="parent"
    app:layout_constraintTop_toTopOf="parent"/>

  <TextView
    android:id="@+id/title"
    android:text="왕십리 시장 탐험을 다녀오다."
    android:textSize="15sp"
    android:textColor="@color/_ffffff"
    android:singleLine="true"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    app:layout_constraintLeft_toLeftOf="@id/my_log_image"
    app:layout_constraintTop_toBottomOf="@+id/my_log_image"
    app:layout_constraintTop_toTopOf="@+id/comma"
    app:layout_constraintBottom_toBottomOf="@+id/comma"/>

  <ImageView
    android:layout_width="0dp"
    android:layout_height="1dp"
    android:background="@color/_686868"
    android:layout_marginTop="6dp"
    app:layout_constraintLeft_toLeftOf="@+id/my_log_image"
    app:layout_constraintRight_toRightOf="parent"
    app:layout_constraintTop_toBottomOf="@+id/title" />
</androidx.constraintlayout.widget.ConstraintLayout>